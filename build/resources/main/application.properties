spring.datasource.url=jdbc:mysql://localhost:3306/myblog?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Enterprise@001

#Hibernate properties
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect
spring.jpa.hibernate.ddl-auto=update

logging.level.org.springframework.security=DEBUG

## App properties
app.jwt-secrets=jwtSecretKey
## 7 Days to exp
app.jwt-expiration-milliseconds=604800000

## AVOID NULL POINTER EXCEPTION AFTER SWAGGER DEP
spring.mvc.pathmatch.matching-strategy = ANT_PATH_MATCHER